{
  "name": "@delta/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Delta — schémas Zod et types partagés (FE/BE/jobs)",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./auth": "./src/auth.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf .turbo coverage"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@delta/config": "workspace:*",
    "eslint": "^9.12.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.2"
  }
}
