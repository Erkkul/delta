{
  "name": "@delta/core",
  "version": "0.0.0",
  "private": true,
  "description": "Delta — logique métier pure (domain). Pas d'I/O.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./auth": "./src/auth/index.ts",
    "./errors": "./src/errors.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf .turbo coverage"
  },
  "dependencies": {
    "@delta/contracts": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@delta/config": "workspace:*",
    "@vitest/coverage-v8": "^2.1.2",
    "eslint": "^9.12.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.2"
  }
}
