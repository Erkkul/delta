{
  "name": "@delta/jobs",
  "version": "0.0.0",
  "private": true,
  "description": "Delta — jobs asynchrones Inngest (handler fins → core).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./inngest-client": "./src/inngest-client.ts",
    "./functions": "./src/functions.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf .turbo coverage"
  },
  "dependencies": {
    "@delta/contracts": "workspace:*",
    "@delta/core": "workspace:*",
    "@delta/db": "workspace:*",
    "inngest": "^3.27.0"
  },
  "devDependencies": {
    "@delta/config": "workspace:*",
    "eslint": "^9.12.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.2"
  }
}
