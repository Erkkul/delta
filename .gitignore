# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Éditeurs
.vscode/
.idea/
*.swp
*.swo

# Node (pour le code à venir)
node_modules/
.env
.env.local
.env.*.local

# Python (pour le backend à venir)
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Build
dist/
build/
*.log

# Claude interne (ne pas versionner les sessions)
.claude/settings.local.json

# Claude Code (config locale, non versionné)
.claude/

# Documentation produit — conservée en local, non versionnée
# Ces fichiers sont accessibles à Claude en local mais n'ont pas leur place dans le repo technique.
# À archiver sur Google Drive si partage externe nécessaire.
PRD.md
vision/etude_marche.md
produit/archives/
